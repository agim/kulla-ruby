source "https://rubygems.org"

gemspec

gem "rake", "~> 13.0"
gem "minitest", "~> 5.25"
gem "rack", ">= 2.2"

# Only for test/rails (run in its own process). Core tests never load Rails.
gem "railties", ">= 7.1", require: false
gem "actionmailer", ">= 7.1", require: false
